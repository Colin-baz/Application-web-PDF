<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="{{ asset('pdf.png') }}" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;700&display=swap" rel="stylesheet">


    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Quicksand', Arial, sans-serif;
        }

        .btn-custom-1 {
            background-color: white;
            color: #7625c2;
            border: 2px solid #7625c2;
        }

        .btn-custom-1:hover {
            background-color: #7625c2;
            color: white;
            border: 2px solid #7625c2;
        }

        .btn-custom-2 {
            background-color: #8A2BE2;
            color: white;
            border: 2px solid #8A2BE2;
        }

        .btn-custom-2:hover {
            background-color: white;
            color: #8A2BE2;
            border: 2px solid #8A2BE2;
        }

    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="/">
            <img src="/pdf.png" alt="Logo" width="40" height="40" class="d-inline-block align-text-top">
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" href="/">Accueil</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Générer un PDF
                    </a>
                    <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <li><a class="dropdown-item" href="{{ path('pdf_generate') }}">URL</a></li>
                        <li><a class="dropdown-item" href="{{ path('generate_pdf_from_html') }}">HTML</a></li>
                        <li><a class="dropdown-item" href="{{ path('generate_pdf_custom') }}">Personnalisé</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('subscription_change') }}">Abonnement</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('history') }}">Historique</a>
                </li>
            </ul>
        </div>

        <!-- Connexion / Inscription ou message de bienvenue -->
        <div class="d-flex align-items-center">
            {% if app.user %}
                <span class="me-3">Bonjour {{ app.user.firstname }} !</span>
                <a href="{{ path('profile') }}" class="nav-link me-3">
                    <i class="fas fa-user-circle fa-2x" style="color: #8A2BE2;"></i>
                </a>
                <a href="{{ path('app_logout') }}" class="btn btn-custom-1">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-custom-1 me-2">Connexion</a>
                <a href="{{ path('app_register') }}" class="btn btn-custom-2">Inscription</a>
            {% endif %}
        </div>
    </div>
</nav>

<div class="container mt-4">
    {% block body %}{% endblock %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
